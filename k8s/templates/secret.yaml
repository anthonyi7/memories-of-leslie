# DO NOT commit real credentials to git.
#
# Create the secret manually before the first deploy:
#
#   kubectl create secret generic memories-postgres-secret \
#     --namespace mol \
#     --from-literal=POSTGRES_USER=leslie \
#     --from-literal=POSTGRES_PASSWORD=<your-password> \
#     --from-literal=DATABASE_URL='postgresql://leslie:<your-password>@postgres-service:5432/leslie'
#
# This file is a reference only. The real Secret is managed out-of-band.
#
# -------------------------------------------------------------------------
# apiVersion: v1
# kind: Secret
# metadata:
#   name: memories-postgres-secret
#   namespace: mol
# type: Opaque
# data:
#   DATABASE_URL: <base64>
#   POSTGRES_USER: <base64>
#   POSTGRES_PASSWORD: <base64>
# -------------------------------------------------------------------------
