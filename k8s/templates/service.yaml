apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-app
  namespace: mol
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-app
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
